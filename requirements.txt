# Generated from pyproject.toml — managed by uv.
# Do not edit manually; run `uv sync` to regenerate.
#
# Install:  uv sync
# Upgrade:  uv lock --upgrade-package playwright && uv sync
#
# Playwright is pinned to an exact version; uv.lock records the full resolved set.

playwright==1.61.0
pytest>=8.0,<9
pytest-playwright==0.8.0
allure-pytest>=2.13,<3
faker>=33.0,<34
pytest-xdist>=3.0,<4
pytest-rerunfailures>=14.0,<15
